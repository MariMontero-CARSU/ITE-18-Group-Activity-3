{
  "name": "ite-18-group-activity-3",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "main.electron.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "electron": "cross-env BUILD_TARGET=electron npm run build && electron .",
    "dist": "cross-env BUILD_TARGET=electron npm run build && electron-builder"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "electron": "^39.0.0",
    "electron-builder": "^26.0.12",
    "gh-pages": "^6.3.0",
    "vite": "^4.0.4"
  },
  "dependencies": {
    "cannon-es": "^0.20.0",
    "lil-gui": "^0.17.0",
    "three": "^0.148.0"
  },
  "build": {
    "appId": "com.carsu.ite18.groupact3",
    "productName": "Pokemon Catcher",
    "directories": {
      "buildResources": "assets",
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "main.electron.cjs",
      "preload.*",
      "package.json"
    ],
    "win": {
      "target": "nsis",
      "artifactName": "PokemonCatcher-${version}-Setup.${ext}"
    },
    "mac": {
      "target": "dmg"
    },
    "linux": {
      "target": "AppImage"
    }
  }
}
